// why do i not need .js at the end?
//whats up with the ./
const peeps = require("./db");
// we =deckae a variable on the LHS
// on the RHS, we call the require() function and pass it the path
// to our dbj.js file, Node is ok if you leave the .js off
console.table(users);
