let my_array = [1, 2, 3, 4, 5]; // can have strings numbers boleans etc
let my_new_array = [];

// use push for append

my_array.push("6");
// pop iss used for removing
my_array.pop("1");

// shift moves an item to the end and unshift
//moves an item to the front

my_array.shift("2");

// to remove use the variable to define the action

let firstItem = myArr.shift();
console.log(firstItem);
console.log(myArr);


let my_array = [1, 2, 3, 4, 5]; 
for(let i = 0; i <  myArr.length; i++){
    console.log(myArr[i])

    // for each is 'for in'

my_array.forEach(function(letter, idx){
//     console.log(letter,idx)
}
let my_array = [1, 2, 3, 4, 5]; 


my_array.forEach(function(letter, idx){
    console.log(letter,idx)
})
// use 

Create a list of letters at with at least 10 items.
print the 4th item.
concatinate the 6th and 3rd item and print the results.
add 4 more letters one at a time to the end of 
the array.
remove the first item of the array.
join all of the letters together and send the 
results to the console.
(bonus) sort the letters!

this_array = 