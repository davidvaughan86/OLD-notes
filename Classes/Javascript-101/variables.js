var myself  = david // assgining immediately. use var to set the name of the variable without var it is incorrect

var myself; // assgining after. you can seperate across multiple lines
myself = david

const // cannot change the a value when using const
const a = 100;

console.log(a) // is the same as using print()

let myself = david // setting varuiable with let

let myCamelCase ="this is how its written with multiple words" // you can use underscores but this is what is accepted

let a,b,c // the way to have multiple values but there is no retuern here because there is no value assigned
console.log(a,b,c)

let a=1,b=2,c=3 // use commas to seperate your values
console.log(a,b,c,)

let david = king;
let diana = Queen;
let ares = dog;

let a,b,c
a = 1
b = 2
c = 3

const david = king
console.log(david)
